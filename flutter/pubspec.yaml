name: zipquantum_flutter_example
description: Auditable Flutter reference for ZipQuantum deep links and deferred deep links.
publish_to: none
version: 0.1.0+1

environment:
  sdk: ">=3.12.0 <4.0.0"

dependencies:
  app_links: 7.2.1
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: 6.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
